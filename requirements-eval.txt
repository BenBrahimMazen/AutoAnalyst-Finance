# Extra dependencies for model evaluation ONLY (scripts/eval_finbert.py).
#
# These are deliberately kept OUT of requirements.txt so the Docker / Modal
# image stays lean — evaluation is a local, reproducibility-only activity and is
# never shipped with the deployed app. Install with:
#
#     pip install -r requirements-eval.txt
#
# (Financial PhraseBank is fetched directly via ``huggingface_hub``, which is
# already pulled in by ``transformers`` in requirements.txt — so no extra dep
# is needed for the dataset itself.)
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
vaderSentiment>=3.3
